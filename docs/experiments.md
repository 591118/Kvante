# Eksperimentlogg

Bruk denne filen som en enkel labbok: dato, hva du kjørte, og hva du la merke til.

## Mal (kopier for hver økt)

- **Dato:**  
- **Kjørte:** `docker run --rm kvante` eller enkeltfil- **Forventning:** (hva tenker du skal skje?)  
- **Resultat:** (tellinger, figurer, feilmeldinger)  
- **Notat:** (spørsmål, hypoteser, neste steg)

---

## Økter

### Eksempel — første gjennomgang

- **Kjørte:** `hello_quantum.py` → `hello_quantum_with_two_bits.py` (via standard `docker run`)  
- **Forventning:** Én qubit: omtrent 50/50 mellom `0` og `1`. To qubits: mest `00` og `11`.  
- **Resultat:** *(fyll inn etter kjøring)*  
- **Notat:** *(valgfritt)*  

Legg til nye blokker under etter hvert som du eksperimenterer (endre gates, shots, nye filer i `app/`).
